import 'dotenv/config'
export const API_KEY = process.env.API_KEY
export const AI_SUFFIX = "generate a podcast script on the topic. The script should be in a natural, conversational style as if I were speaking it directly. Please provide only the spoken text, without any special formatting, code examples, or extraneous symbols.";
